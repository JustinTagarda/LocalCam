<Window x:Class="LocalCam.StoreUpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="App Update"
        Width="420"
        Height="220"
        MinWidth="420"
        MinHeight="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Icon="pack://application:,,,/Assets/icon.ico"
        WindowStyle="ToolWindow"
        Background="#101722">
    <Border Margin="0"
            Background="#101722"
            BorderBrush="#2A3242"
            BorderThickness="1">
        <Grid Margin="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Microsoft Store update"
                       Foreground="#E8EEF5"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10" />

            <TextBlock x:Name="PhaseTextBlock"
                       Grid.Row="1"
                       Foreground="#E8EEF5"
                       FontSize="12"
                       Text="Preparing"
                       Margin="0,0,0,6" />

            <ProgressBar x:Name="ProgressBar"
                         Grid.Row="2"
                         Height="8"
                         Minimum="0"
                         Maximum="100"
                         Value="0"
                         Margin="0,0,0,8" />

            <TextBlock x:Name="DetailTextBlock"
                       Grid.Row="3"
                       Foreground="#8FA2BE"
                       FontSize="11"
                       TextWrapping="Wrap"
                       Margin="0,0,0,4"
                       Visibility="Collapsed" />

            <TextBlock x:Name="ResultTextBlock"
                       Grid.Row="4"
                       Foreground="#8FA2BE"
                       FontSize="11"
                       TextWrapping="Wrap"
                       VerticalAlignment="Top"
                       Visibility="Collapsed" />

            <Button x:Name="CloseButton"
                    Grid.Row="5"
                    Content="Close"
                    Width="82"
                    Height="30"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0"
                    IsEnabled="False"
                    Click="CloseButton_Click" />
        </Grid>
    </Border>
</Window>
