<Window x:Class="LocalCam.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="LocalCam - Tapo Discovery" Height="620" Width="1100" MinHeight="500" MinWidth="900">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Tapo Camera Discovery"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="ScanButton"
                    Content="Scan Network"
                    Width="130"
                    Height="32"
                    Click="ScanButton_Click" />
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Width="100"
                    Height="32"
                    Margin="8,0,0,0"
                    Click="CancelButton_Click"
                    IsEnabled="False" />
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="220" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusText"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Text="Ready to scan."
                       TextWrapping="Wrap" />

            <ProgressBar x:Name="ScanProgress"
                         Grid.Column="1"
                         Height="16"
                         Margin="12,0,0,0"
                         IsIndeterminate="True"
                         Visibility="Collapsed" />
        </Grid>

        <DataGrid x:Name="CameraDataGrid"
                  Grid.Row="3"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="130" />
                <DataGridTextColumn Header="Host Name" Binding="{Binding HostName}" Width="180" />
                <DataGridTextColumn Header="MAC Address" Binding="{Binding MacAddress}" Width="150" />
                <DataGridTextColumn Header="Open Ports" Binding="{Binding OpenPorts}" Width="120" />
                <DataGridTextColumn Header="Confidence" Binding="{Binding Confidence}" Width="100" />
                <DataGridTextColumn Header="Reason" Binding="{Binding DetectionReason}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
