namespace LocalCam.Models {
    internal enum StoreRedemptionOutcome {
        OpenedRedeemPage,
        Completed,
        Cancelled,
        InvalidCode,
        Unavailable,
        Failed
    }
}
